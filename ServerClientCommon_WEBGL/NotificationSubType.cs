namespace ServerClientCommon {
    public enum NotificationSubType : int {
        Unknown = 0,
        InviteToGroup,
        InviteToGuild,
        RequestToGuild,
        RequestFriend,
        RequestToGroup,
        MiningStationAttack,
        AuctionPurchase
    }
}
