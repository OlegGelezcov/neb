using System;
using System.Collections.Generic;
using System.Linq;
using System.Text;

namespace DSL.Lang {
    public class ASTGenerator {

        //private List<StatementNode> statements = new List<StatementNode>();


        //public AST Generate(TokenStream stream) {
        //    stream.Reset();

        //    StatementListNode root = new StatementListNode();
        //    AST current = root;

        //    while (stream.current.type != Token.TokenType.INVALID) {
        //        //switch()
        //    }


        //}

        //private ExpressionNode ParseExpression(TokenStream stream) {
        //    int i = 0;
        //    while(stream.Forward(i).type != Token.TokenType.STAT_END) {

        //    }
        //}

        //private bool IsBinaryOperator(Token token) {
        //    switch(token.type) {
        //        case Token.TokenType.MULT:
        //            case Token.TokenType.
        //    }
        //}
        
    }
}
