using System;
using System.Collections.Generic;
using System.Linq;
using System.Text;
using Common;

namespace Space.Game.Skills
{
    //public class SkillStorage
    //{
    //    private List<SkillInfo> _skills;

    //    public SkillStorage(List<SkillInfo> skills) {
    //        _skills = skills;
    //    }

    //    public List<SkillInfo> Skills(ShipModelSlotType type) {
    //        return _skills.Where(s => s.type == type).ToList();
    //    }

    //    public SkillInfo Skill(string id) {
    //        return _skills.Where(s => s.id == id).FirstOrDefault();
    //    }

    //    public override string ToString()
    //    {
    //        var sb = new StringBuilder();
    //        _skills.ForEach(s =>
    //        {
    //            sb.AppendLine(s.ToString());
    //        });
    //        return sb.ToString();
    //    }
    //}
}
