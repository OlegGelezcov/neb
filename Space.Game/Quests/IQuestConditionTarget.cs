/*
using Common;
using GameMath;
using Nebula.Quests.Actions;
using Space.Game;
using System;
using System.Collections.Generic;
using System.Linq;
using System.Text;

namespace Nebula.Quests {
    public interface IQuestConditionTarget {
        bool isSpace();
        bool isStation();
        bool isQuestCompleted(string id);
        bool isDialogCompleted(string id);
        int CountOfItems(InventoryObjectType itemType, string itemId);
        bool isWorld(string worldId);
        bool HasNearActivatorsWithBadge(string badge, bool sendError = true);
        bool NearPoint(Vector3 point, float radius);
        void IncreaseInteger(string varName, int cnt);
        void SetFloatVariable(string name, float val);
        bool SetBoolVariable(string name, bool val);
        void ExecutePostActionList(List<PostAction> postActions);
        bool TryCheckActiveQuests(object userData = null);
        Race GetRace();
        IRes GetResource();
    }
}
*/
